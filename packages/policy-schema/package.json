{
  "name": "@cg/policy-schema",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./contract/*": "./contract/*"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "echo 'no build step — consumed as TypeScript source'",
    "test": "bun test",
    "generate": "bun scripts/generate-hook-contract.ts --write",
    "generate:check": "bun scripts/generate-hook-contract.ts --check",
    "fetch": "bun scripts/generate-hook-contract.ts --fetch"
  },
  "dependencies": {
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "typescript": "^7.0.2"
  }
}
